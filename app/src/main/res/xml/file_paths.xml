<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Rendered shareable reports live in cacheDir/reports and are exposed via FileProvider. -->
    <cache-path name="reports" path="reports/" />
</paths>
